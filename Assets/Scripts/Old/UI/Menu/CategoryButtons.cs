using System;
using UnityEngine;

/*
public class CategoryButtons : MonoBehaviour
{
    public SkinsPanel skins_panel;
    public SkinCategory category;
    public EventType myEvent = EventType.no;

    private void OnEnable()
    {
        if (myEvent != EventType.no)
        {
            if (!ManagerScript.AccessEvent(myEvent))
            {
                Destroy(gameObject);
            }
        }
    }

    public void ChangeCategory()
    {
        skins_panel.UpdateSkins(false, category);
    }
}
*/