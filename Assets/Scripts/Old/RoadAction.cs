using UnityEngine;
/*
public enum ActionWithRoad
{
    create,
    destroy
}

public class RoadAction : MonoBehaviour
{
    public RoadGenerator parent_road;
    public ActionWithRoad action;

    private void OnTriggerEnter2D(Collider2D collision)
    {
        if (collision.tag == "Player")
        {
            switch (action)
            {
                case ActionWithRoad.create:
                    parent_road.GenerateRoad();
                    break;

                case ActionWithRoad.destroy:
                    Destroy(parent_road.gameObject);
                    break;
            }
        }
    }
}
*/